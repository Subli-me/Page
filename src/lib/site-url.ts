// URL pública del sitio, usada para metadata absoluta (Open Graph, sitemap,
// canonical). Configurable por env var para cuando tengan dominio propio.
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL || "https://subli-me.vercel.app";
